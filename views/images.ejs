
<% if (locals.waifu) { %>
    <% console.log("This is the ejs file: \n" + waifu); %>
    <% waifu.forEach((element) => { %>
    <img src="<%= element.url %>" class="image" alt="Anime waifu" >
    <% }); %>
<% } %>